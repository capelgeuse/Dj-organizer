# Tauri icons

Icons are generated from `Capelhouse/logo.png` during the packaging phase. The
source tree intentionally does not contain a placeholder icon; P10 must fail
closed until the production icon set exists.
